import UIKit
import RxSwift
import ReactorKit

public final class {{name}}ViewController: UIViewController {
  // MARK: - Properties
  var reactor: {{name}}Reactor?

  Override func viewDidLoad() {
    super.viewDidLoad()
    reactor = {{name}}Reactor()
    // 코드작성 고고링~

  }
}

// MARK: - View
extension {{name}}ViewController: View {
  func bind(reactor: {{name}}Reactor) {
  
  }
}